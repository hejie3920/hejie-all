import Vue from 'vue'
import ElementUI from 'element-ui'
// import { Button, Form, FormItem, Input, Table } from 'element-ui'

export default {
  install() {
    Vue.use(ElementUI)
    // Vue.use(Button)
    // Vue.use(Form)
    // Vue.use(FormItem)
    // Vue.use(Input)
    // Vue.use(Table)
  }
}
