import React, { Component } from "react"
// import PropTypes from "prop-types"

class Mobx extends Component {
  render() {
    return (
      <div>
        <h1>Mobx</h1>
      </div>
    )
  }
}

// Mobx.propTypes = {}

export default Mobx
